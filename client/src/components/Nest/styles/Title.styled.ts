import styled from 'styled-components';

export const Title = styled.h3`
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    background-color: rgba(253, 101, 145, 1);
    padding: 0.5em 0.75em 1.2em;
    color: #fff;
    margin: 0;
`;